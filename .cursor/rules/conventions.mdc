---
description: Конвенции разработки — KISS, агентный цикл, структура, async
alwaysApply: true
---

# Conventions

Источник истины: `docs/vision.md`. Стек, зависимости, команды запуска — только там.

## Scope

- Цепочка: Telegram → `AgentService` → агентный цикл (LLM + tools) → ответ.
- Инструменты: `rag_search` (ChromaDB), `web_search` (Tavily), `capture_lead` (SQLite).
- Persistent-хранилища — только RAG (`data/chroma`) и лиды (`data/leads.db`).
- История диалога — в памяти процесса, последние 20 пар user/assistant на пользователя.
- Без очередей, web-фреймворков, микросервисов, LangChain и тяжёлых AI-фреймворков.

## Код

- Python 3.11, весь I/O — `async`/`await`.
- 1 класс = 1 файл в `src/`, имя файла — snake_case от класса (`LlmClient` → `llm_client.py`).
- Зависимости — через конструктор; сборка объектов — в `main.py`.
- Агентный цикл явный: tool calling через openai SDK, без скрытых оркестраторов.
- **LangSmith обязателен**: ключевые async-методы (`AgentService`, `LlmClient`, инструменты) — с `@traceable` из `langsmith`; новые шаги цикла и tools — тоже.
- Классы и зоны ответственности — см. vision.md §2 (`Bot`, `AgentService`, `LlmClient`, `EmbeddingClient`, `RagService`, `WebSearchTool`, `LeadStore`, `Config`).

## Секреты и конфигурация

- Параметры и секреты — только из `.env` (через `Config`, `python-dotenv`).
- Системный промпт — `prompts/system.txt`, путь задаётся в `.env` (`SYSTEM_PROMPT_FILE`).
- LangSmith: `LANGSMITH_ENABLED`, `LANGSMITH_API_KEY`, `LANGSMITH_PROJECT` — см. vision.md §1 «Трейсинг (LangSmith)`.
- Хардкод токенов, ключей и настроек в коде запрещён.
- `.env` и любые файлы с секретами не коммитить и не пушить в GitHub.
- В коде и примерах — плейсхолдеры, не реальные значения.

## Запреты

- Абстрактные базовые классы, фабрики и паттерны «на будущее».
- LangChain, LlamaIndex и аналоги.
- Новые зависимости без явного запроса (разрешены только из vision.md).
- Тестовая инфраструктура на старте.

## При генерации кода

- Минимальный diff: только то, что нужно для задачи.
- Следовать существующим именам и структуре проекта.
- Комментарии — только для неочевидной логики.
